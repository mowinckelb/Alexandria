import MarkdownDoc from '../components/MarkdownDoc';

export default function MechanicsPage() {
  return (
    <MarkdownDoc
      src="/docs/Mechanics.md"
      header="how the system actually works. ~5 min."
      homeHref="/"
    />
  );
}
